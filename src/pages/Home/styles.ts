import styled from 'styled-components'

export const HomeMainContainer = styled.main`
  height: 33.8rem;
  padding-top: 8rem;
  padding-left: 2rem;

  h1 {
    font-weight: 500;
    width: 25rem;
    line-height: 1.5;
  }

  span {
    color: #002abf;
    font-weight: 900;
  }
`
